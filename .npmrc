auto-install-peers=false
strict-peer-dependencies=false

# OneDrive / cloud-synced folders cannot use hard links and often block temp
# files in the project tree. Copy packages instead of linking into node_modules.
package-import-method=copy

# Avoid pre-run install checks that write _tmp_* files into the repo root.
verify-deps-before-run=false
